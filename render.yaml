services:
  - type: worker
    name: grandline-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: VOUCH_CHANNEL
        value: 0
      - key: LOG_CHANNEL
        value: 0
